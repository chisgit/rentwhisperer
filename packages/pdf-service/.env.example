# PDF Service Environment Variables

# Server Configuration
# PORT=3001 (default, will fallback to 3002 if 3001 is in use)
NODE_ENV=development

# Supabase Configuration (if needed directly by PDF service)
SUPABASE_URL=
SUPABASE_KEY=

# Backend API Endpoint (if PDF service calls backend)
BACKEND_API_URL=http://localhost:3000/api

LOG_LEVEL=debug
